#include <stdio.h>

int main () {
  int a, b, c;
  scanf("%d %d", &a, &b);
  
  c = a + b;
  
  printf("%d", c);
}

// Progam to test if a C program accepts any input via scanf 
// irrespective of the variable type